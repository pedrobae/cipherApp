import 'package:flutter/material.dart';

class PlayLocalVersion extends StatelessWidget {
  final int versionId;

  const PlayLocalVersion({super.key, required this.versionId});

  @override
  Widget build(BuildContext context) {
    return Container(); // TODO : Implement play local version UI
  }
}
